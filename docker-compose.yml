version: '3.8'

services:
  freqtrade-dashboard:
    build: .
    container_name: freqtrade-neobroker-dashboard
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
